# 构建时依赖 - 解决PEP 517兼容性问题

# 核心构建工具
pip>=23.0
setuptools>=61.0
wheel>=0.38.0
build>=0.10.0

# 平台特定构建工具
py2app>=0.28; sys_platform == "darwin"
pyinstaller>=5.13; sys_platform != "darwin"

# 应用运行时依赖
PySide6>=6.5.0,<7.0.0
emoji>=2.2.0,<3.0.0
platformdirs>=3.0.0,<5.0.0